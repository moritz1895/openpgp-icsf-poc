# In dieser Iteration existiert bewusst noch kein @DrivingAdapter (der
# CLI-Demo-Adapter folgt erst in einer Folge-Iteration, siehe Projektplan).
# Ohne diese Einstellung wuerde die ArchUnit-Regel
# "drivingAdaptersMustNotDependOnApplicationServices" fehlschlagen, weil sie
# aktuell keine einzige Klasse zu pruefen findet (vacuously true). Sobald der
# CLI-Adapter existiert, greift die Regel wieder normal.
archRule.failOnEmptyShould=false
